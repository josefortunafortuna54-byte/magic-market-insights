# Internacionalização (i18n) — 14 idiomas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpawers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar a app TMT traduzível para 14 idiomas (pt, en, es, fr, ja, ln, de, it, nl, zh, ko, ru, sw, ar) com deteção do idioma do dispositivo, escolha manual persistida no Perfil e re-render instantâneo.

**Architecture:** i18next + react-i18next com dicionários JSON (`src/lib/i18n/locales/*.json`), deteção inicial via `expo-localization` `getLocales()`, persistência da escolha em AsyncStorage (chave `app_language`), e um modal `/idioma` acessível a partir do Perfil. `pt.json` é a fonte de todas as chaves; os restantes 13 ficheiros são gerados a partir dele. Todas as strings PT hardcoded (23 ecrãs, 20 componentes, libs de notificações, core/format) passam a `t('...')`.

**Tech Stack:** Expo SDK 56 (`~56.0.19`), React Native 0.85.3, expo-router `~56.2.18`, `i18next`, `react-i18next`, `expo-localization`, `@react-native-async-storage/async-storage` (já instalado, 2.2.0).

**Spec:** `.superpawers/specs/2026-08-15-i18n-design.md`
**Branch:** `feature/i18n` (já criada; baseline `npx tsc --noEmit` limpo)

---

## Convenções globais

- **`pt.json` é a fonte.** Todas as chaves são adicionadas primeiro a `pt.json`. Os outros 13 ficheiros são gerados na Task 18.
- **Estrutura de chaves:** aninhada por área (`common`, `tabs`, `inicio`, `analises`, `historico`, `horarios`, `comunidade`, `perfil`, `planos`, `banca`, `definicoesBooms`, `notificacoes`, `sinal`, `planoCrescimento`, `suporteIa`, `auth`, `legal`, `admin`, `components`, `notifications`, `format`, `language`).
- **Interpolação i18next:** usar `{{chave}}` dentro dos valores JSON.
- **Import do `t`:** em componentes, `const { t } = useTranslation();`. Em módulos fora de React (lib/core), `import { i18n } from '@/lib/i18n'` e `i18n.t('...')`.
- **Datas:** `src/core/format.ts` passa a usar `i18n.t` para meses e padrões de data. Os `toLocaleDateString('pt-PT')` espalhados pelos ecrãs passam a `new Date(x).toLocaleDateString(i18n.language)` (sem mudar formato).
- **Números/moedas:** mantêm-se (`formatMoney` continua `en-US`).
- **Nomes de canais Android de notificações:** são definidos uma vez por instalação; traduzem-se no momento de criação com o idioma ativo.
- **Árabe em LTR:** sem `I18nManager`.
- **Verificação em cada task:** `npx tsc --noEmit` + `npx expo lint` (0 erros) + commit.
- **Rutas tipadas:** `experiments.typedRoutes: true` — depois de criar a rota `/idioma`, correr o dev server (ou `npx expo export --platform web`) uma vez para regenerar `.expo/types/router.d.ts` antes de `tsc`.

---

### Task 1: Instalar dependências e configurar plugin

**Files:**
- Modify: `package.json` (via `npx expo install`)
- Modify: `app.json`

- [ ] **Step 1: Instalar dependências**

Run: `npx expo install i18next react-i18next expo-localization`
Expected: `package.json` ganha `i18next`, `react-i18next`, `expo-localization` com versões compatíveis com SDK 56.

- [ ] **Step 2: Adicionar plugin ao app.json**

`app.json` — dentro de `expo.plugins`, adicionar `"expo-localization"`:

```json
{
  "expo": {
    "plugins": [
      "expo-router",
      "expo-splash-screen",
      "expo-audio",
      "expo-notifications",
      "expo-web-browser",
      "expo-localization"
    ]
  }
}
```

Nota: o plugin exige rebuild do dev client para efeito completo (iOS `locales`); a API runtime (`getLocales()`) já funciona no build atual.

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` (esperado: limpo)
Run: `npx expo lint` (esperado: 0 erros)
```bash
git add package.json package-lock.json app.json
git commit -m "chore: instalar i18next, react-i18next, expo-localization"
```

---

### Task 2: Módulo core de i18n (init, deteção, persistência, línguas)

**Files:**
- Create: `src/lib/i18n/languages.ts`
- Create: `src/lib/i18n/index.ts`
- Create: `src/lib/i18n/i18next.d.ts`
- Create: `src/lib/i18n/locales/pt.json`

- [ ] **Step 1: Criar `src/lib/i18n/languages.ts`**

```ts
export interface Language {
  code: string;
  nativeName: string;
  flag: string;
}

export const LANGUAGES: Language[] = [
  { code: 'pt', nativeName: 'Português', flag: '🇵🇹' },
  { code: 'en', nativeName: 'English', flag: '🇬🇧' },
  { code: 'es', nativeName: 'Español', flag: '🇪🇸' },
  { code: 'fr', nativeName: 'Français', flag: '🇫🇷' },
  { code: 'ja', nativeName: '日本語', flag: '🇯🇵' },
  { code: 'ln', nativeName: 'Lingála', flag: '🇨🇩' },
  { code: 'de', nativeName: 'Deutsch', flag: '🇩🇪' },
  { code: 'it', nativeName: 'Italiano', flag: '🇮🇹' },
  { code: 'nl', nativeName: 'Nederlands', flag: '🇳🇱' },
  { code: 'zh', nativeName: '中文', flag: '🇨🇳' },
  { code: 'ko', nativeName: '한국어', flag: '🇰🇷' },
  { code: 'ru', nativeName: 'Русский', flag: '🇷🇺' },
  { code: 'sw', nativeName: 'Kiswahili', flag: '🇹🇿' },
  { code: 'ar', nativeName: 'العربية', flag: '🇸🇦' },
];

export const SUPPORTED_CODES: string[] = LANGUAGES.map((l) => l.code);
```

- [ ] **Step 2: Criar `src/lib/i18n/index.ts`**

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { getLocales } from 'expo-localization';
import { SUPPORTED_CODES } from './languages';

export const STORAGE_KEY = 'app_language';

const resources = {
  pt: { translation: require('./locales/pt.json') },
  en: { translation: require('./locales/en.json') },
  es: { translation: require('./locales/es.json') },
  fr: { translation: require('./locales/fr.json') },
  ja: { translation: require('./locales/ja.json') },
  ln: { translation: require('./locales/ln.json') },
  de: { translation: require('./locales/de.json') },
  it: { translation: require('./locales/it.json') },
  nl: { translation: require('./locales/nl.json') },
  zh: { translation: require('./locales/zh.json') },
  ko: { translation: require('./locales/ko.json') },
  ru: { translation: require('./locales/ru.json') },
  sw: { translation: require('./locales/sw.json') },
  ar: { translation: require('./locales/ar.json') },
};

export function detectDeviceLanguage(): string {
  try {
    const code = getLocales()[0]?.languageCode ?? null;
    if (code && SUPPORTED_CODES.includes(code)) return code;
  } catch {
    // ignore
  }
  return 'pt';
}

export function initI18n(): typeof i18n {
  if (i18n.isInitialized) return i18n;
  i18n.use(initReactI18next).init({
    resources,
    lng: detectDeviceLanguage(),
    fallbackLng: 'pt',
    returnNull: false,
    interpolation: { escapeValue: false },
  });
  return i18n;
}

export { i18n };

export async function getStoredLanguage(): Promise<string | null> {
  try {
    const stored = await AsyncStorage.getItem(STORAGE_KEY);
    return stored && SUPPORTED_CODES.includes(stored) ? stored : null;
  } catch {
    return null;
  }
}

export async function setLanguage(code: string): Promise<void> {
  if (!SUPPORTED_CODES.includes(code)) return;
  await i18n.changeLanguage(code);
  try {
    await AsyncStorage.setItem(STORAGE_KEY, code);
  } catch {
    // ignore
  }
}
```

Nota: os `require('./locales/en.json')` etc. resolvem no Metro porque os nomes são literais. A Task 18 cria esses ficheiros; até lá o fallback é `pt` (sem erros — o require só falha se o ficheiro não existir em runtime; ver Task 18 Step 0 para criar ficheiros vazios válidos se necessário).

**IMPORTANTE — Task 18 Step 0:** criar já ficheiros mínimos válidos (`{ "common": { "loading": "" } }`) para os 13 idiomas para não quebrar o Metro antes da geração completa. (O `require` é resolvido em build, logo ficheiros em falta fazem o bundle falhar.)

- [ ] **Step 3: Criar `src/lib/i18n/i18next.d.ts`**

```ts
import 'i18next';
import pt from './locales/pt.json';

declare module 'i18next' {
  interface CustomTypeOptions {
    defaultNS: 'translation';
    resources: { translation: typeof pt };
  }
}
```

Nota: `"resolveJsonModule"` já vem ativo no tsconfig do Expo (verificar; se faltar, adicionar ao `tsconfig.json` `compilerOptions`).

- [ ] **Step 4: Criar `src/lib/i18n/locales/pt.json` (seed — base da app toda; as Tasks seguintes acrescentam secções)**

```json
{
  "common": {
    "loading": "A carregar…",
    "cancel": "Cancelar",
    "save": "Guardar",
    "send": "Enviar",
    "retry": "Tentar novamente",
    "close": "Fechar",
    "confirm": "Confirmar",
    "ok": "OK",
    "comingSoon": "Funcionalidade em breve.",
    "all": "Todos",
    "trader": "Trader",
    "waiting": "AGUARDAR"
  },
  "language": {
    "title": "Idioma"
  }
}
```

- [ ] **Step 5: Criar os 13 ficheiros mínimos de locale (placeholder)**

Para cada código em `es, fr, ja, ln, de, it, nl, zh, ko, ru, sw, ar` + `en`, criar `src/lib/i18n/locales/<code>.json` com:

```json
{
  "common": {
    "loading": ""
  }
}
```

Run (PowerShell) para criar os 14 (incluindo en):
```powershell
@('en','es','fr','ja','ln','de','it','nl','zh','ko','ru','sw','ar') | ForEach-Object {
  $p = "src/lib/i18n/locales/$_.json"
  if (-not (Test-Path -LiteralPath $p)) {
    Set-Content -LiteralPath $p -Value '{ "common": { "loading": "" } }' -Encoding UTF8
  }
}
```

- [ ] **Step 6: Verificação + commit**

Run: `npx tsc --noEmit` (esperado: limpo; `i18next.d.ts` valida as chaves de `pt.json`)
Run: `npx expo lint` (esperado: 0 erros)
```bash
git add src/lib/i18n
git commit -m "feat(i18n): modulo core com deteção, persistência e 14 locales (seed pt)"
```

---

### Task 3: Ligar i18n ao root layout + traduzir títulos de ecrã

**Files:**
- Modify: `src/app/_layout.tsx`

- [ ] **Step 1: Init + sincronização de idioma no `_layout.tsx`**

Substituir o conteúdo de `src/app/_layout.tsx` por:

```tsx
import { useEffect } from 'react';
import { AppState } from 'react-native';
import { Stack } from 'expo-router';
import { StatusBar } from 'expo-status-bar';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { useTranslation } from 'react-i18next';
import { AuthProvider } from '@/hooks/useAuth';
import { AiFab } from '@/components/AiFab';
import { useNotificationNavigation, useUpgradeNotification } from '@/hooks/useNotifications';
import { ensureChannel } from '@/lib/notifications';
import { Colors } from '@/core/theme';
import { initI18n, getStoredLanguage, detectDeviceLanguage, i18n } from '@/lib/i18n';
import { SUPPORTED_CODES } from '@/lib/i18n/languages';

initI18n();

const queryClient = new QueryClient({
  defaultOptions: {
    queries: { retry: 1, staleTime: 30_000 },
  },
});

function NotificationInit() {
  useUpgradeNotification();
  return null;
}

function LanguageSync() {
  useEffect(() => {
    let mounted = true;
    const apply = () => {
      getStoredLanguage().then((stored) => {
        if (!mounted) return;
        if (stored) {
          if (i18n.language !== stored) i18n.changeLanguage(stored);
        } else {
          const device = detectDeviceLanguage();
          if (i18n.language !== device) i18n.changeLanguage(device);
        }
      });
    };
    apply();
    const sub = AppState.addEventListener('change', (state) => {
      if (state === 'active') apply();
    });
    return () => {
      mounted = false;
      sub.remove();
    };
  }, []);
  return null;
}

export default function RootLayout() {
  const { t } = useTranslation();
  useNotificationNavigation();

  useEffect(() => {
    ensureChannel().catch(() => {});
  }, []);

  return (
    <QueryClientProvider client={queryClient}>
      <AuthProvider>
        <LanguageSync />
        <NotificationInit />
        <StatusBar style="light" />
        <Stack
          screenOptions={{
            headerStyle: { backgroundColor: Colors.bg },
            headerTintColor: Colors.text,
            headerShadowVisible: false,
            contentStyle: { backgroundColor: Colors.bg },
          }}>
          <Stack.Screen name="(auth)" options={{ headerShown: false }} />
          <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
          <Stack.Screen name="sinal/[id]" options={{ title: t('sinal.title') }} />
          <Stack.Screen name="planos" options={{ title: t('planos.title'), presentation: 'modal' }} />
          <Stack.Screen name="banca" options={{ title: t('banca.title') }} />
          <Stack.Screen name="notificacoes" options={{ title: t('notificacoes.title') }} />
          <Stack.Screen name="definicoes-booms" options={{ title: t('definicoesBooms.title'), presentation: 'modal' }} />
          <Stack.Screen name="suporte-ia" options={{ title: t('suporteIa.title'), presentation: 'modal', headerShown: false }} />
          <Stack.Screen name="plano-crescimento" options={{ title: t('planoCrescimento.title') }} />
          <Stack.Screen name="idioma" options={{ title: t('language.title'), presentation: 'modal' }} />
          <Stack.Screen name="termos" options={{ title: t('legal.termosTitle') }} />
          <Stack.Screen name="privacidade" options={{ title: t('legal.privacidadeTitle') }} />
          <Stack.Screen name="aviso-risco" options={{ title: t('legal.avisoRiscoTitle') }} />
        </Stack>
        <AiFab />
      </AuthProvider>
    </QueryClientProvider>
  );
}
```

Nota: `SUPPORTED_CODES` é importado mas não usado — **removê-lo** do import se o lint reclamar (`no-unused-vars`).

- [ ] **Step 2: Acrescentar as chaves de títulos ao `pt.json`**

Acrescentar a `src/lib/i18n/locales/pt.json`:

```json
{
  "sinal": { "title": "Análise Completa" },
  "planos": { "title": "Planos" },
  "banca": { "title": "Gestão de Banca" },
  "notificacoes": { "title": "Notificações" },
  "definicoesBooms": { "title": "Definições do Boom" },
  "suporteIa": { "title": "Suporte IA" },
  "planoCrescimento": { "title": "Plano de Crescimento" },
  "legal": {
    "termosTitle": "Termos de Uso",
    "privacidadeTitle": "Privacidade",
    "avisoRiscoTitle": "Aviso de Risco"
  }
}
```

- [ ] **Step 3: Regenerar typed routes + verificação + commit**

Run: `npx expo start` (esperar o servidor arrancar ~10s; Ctrl+C)
Run: `npx tsc --noEmit` (esperado: limpo)
Run: `npx expo lint` (esperado: 0 erros)
```bash
git add src/app/_layout.tsx src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): init + LanguageSync no root layout e títulos de ecrã"
```

---

### Task 4: Traduzir `src/core/format.ts`

**Files:**
- Modify: `src/core/format.ts`
- Modify: `src/lib/i18n/locales/pt.json`

- [ ] **Step 1: Acrescentar chaves `format` ao `pt.json`**

```json
{
  "format": {
    "now": "agora mesmo",
    "minutesAgo": "há {{count}}min",
    "hoursAgo": "há {{count}}h",
    "daysAgo": "há {{count}}d",
    "longDate": "{{day}} de {{month}} de {{year}}",
    "datetimeWAT": "{{dd}}/{{mm}}/{{yyyy}} às {{hh}}:{{min}} WAT",
    "months": {
      "january": "Janeiro",
      "february": "Fevereiro",
      "march": "Março",
      "april": "Abril",
      "may": "Maio",
      "june": "Junho",
      "july": "Julho",
      "august": "Agosto",
      "september": "Setembro",
      "october": "Outubro",
      "november": "Novembro",
      "december": "Dezembro"
    }
  }
}
```

- [ ] **Step 2: Alterar `src/core/format.ts`**

Substituir o bloco `timeAgo`:

```ts
import { i18n } from '@/lib/i18n';

export function timeAgo(dateStr: string): string {
  const diff = (Date.now() - new Date(dateStr).getTime()) / 1000;
  if (diff < 60) return i18n.t('format.now');
  if (diff < 3600) return i18n.t('format.minutesAgo', { count: Math.floor(diff / 60) });
  if (diff < 86400) return i18n.t('format.hoursAgo', { count: Math.floor(diff / 3600) });
  return i18n.t('format.daysAgo', { count: Math.floor(diff / 86400) });
}
```

Remover `MONTHS_PT` e alterar `formatLongDate` e `formatDateTimeWAT`:

```ts
const MONTH_KEYS = [
  'january', 'february', 'march', 'april', 'may', 'june',
  'july', 'august', 'september', 'october', 'november', 'december',
] as const;

export function formatLongDate(dateStr: string): string {
  const d = new Date(dateStr);
  return i18n.t('format.longDate', {
    day: d.getDate(),
    month: i18n.t(`format.months.${MONTH_KEYS[d.getMonth()]}`),
    year: d.getFullYear(),
  });
}

export function formatDateTimeWAT(dateStr: string): string {
  const d = new Date(dateStr);
  const dd = String(d.getDate()).padStart(2, '0');
  const mm = String(d.getMonth() + 1).padStart(2, '0');
  const hh = String(d.getHours()).padStart(2, '0');
  const min = String(d.getMinutes()).padStart(2, '0');
  return i18n.t('format.datetimeWAT', { dd, mm, yyyy: d.getFullYear(), hh, min });
}
```

Alterar `displayName` (linhas com `'Trader'`):

```ts
  if (isSyntheticEmail(email)) return i18n.t('common.trader');
  return email.split('@')[0] || i18n.t('common.trader');
```

`formatSymbol`, `formatNumber`, `formatPrice`, `formatPips`, `formatMoney`, `pad2`, `avatarLetter` **mantêm-se** (símbolos e números são dados; `'N/A'` e `'—'` ficam).

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` (esperado: limpo)
Run: `npx expo lint` (esperado: 0 erros)
```bash
git add src/core/format.ts src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): format.ts (timeAgo, meses, datas) localizado"
```

---

### Task 5: Modal `/idioma` + entrada no Perfil

**Files:**
- Create: `src/app/idioma.tsx`
- Modify: `src/app/(tabs)/perfil.tsx` (apenas a linha "Preferências")
- Modify: `src/lib/i18n/locales/pt.json`

- [ ] **Step 1: Criar `src/app/idioma.tsx`**

```tsx
import { useState } from 'react';
import { FlatList, Pressable, StyleSheet, View } from 'react-native';
import { router } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useTranslation } from 'react-i18next';
import { AppText } from '@/components/ui';
import { Colors, Spacing } from '@/core/theme';
import { LANGUAGES } from '@/lib/i18n/languages';
import { setLanguage } from '@/lib/i18n';

export default function IdiomaScreen() {
  const { t, i18n } = useTranslation();
  const [saving, setSaving] = useState(false);

  const onSelect = async (code: string) => {
    if (saving) return;
    if (code === i18n.language) {
      router.back();
      return;
    }
    setSaving(true);
    await setLanguage(code);
    setSaving(false);
    router.back();
  };

  return (
    <SafeAreaView style={styles.safe} edges={['top', 'bottom']}>
      <View style={styles.header}>
        <Pressable
          onPress={() => router.back()}
          accessibilityRole="button"
          accessibilityLabel={t('common.close')}
          hitSlop={8}>
          <Ionicons name="close" size={26} color={Colors.text} />
        </Pressable>
        <AppText variant="title" style={styles.headerTitle}>{t('language.title')}</AppText>
        <View style={styles.spacer} />
      </View>
      <FlatList
        data={LANGUAGES}
        keyExtractor={(l) => l.code}
        contentContainerStyle={styles.list}
        renderItem={({ item }) => (
          <Pressable
            style={({ pressed }) => [styles.row, pressed && { backgroundColor: Colors.surfaceElevated }]}
            onPress={() => onSelect(item.code)}
            accessibilityRole="button">
            <AppText style={styles.flag}>{item.flag}</AppText>
            <AppText style={styles.name}>{item.nativeName}</AppText>
            {i18n.language === item.code && <Ionicons name="checkmark-circle" size={22} color={Colors.accent} />}
          </Pressable>
        )}
      />
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: Colors.bg },
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingHorizontal: Spacing.lg,
    paddingVertical: Spacing.md,
    borderBottomColor: Colors.border,
    borderBottomWidth: StyleSheet.hairlineWidth,
  },
  headerTitle: { flex: 1, textAlign: 'center', marginHorizontal: Spacing.sm },
  spacer: { width: 26 },
  list: { padding: Spacing.lg, gap: Spacing.sm },
  row: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: Spacing.md,
    padding: Spacing.md,
    backgroundColor: Colors.surface,
    borderColor: Colors.border,
    borderWidth: 1,
    borderRadius: 14,
  },
  flag: { fontSize: 22 },
  name: { flex: 1, fontSize: 16, color: Colors.text },
});
```

- [ ] **Step 2: Substituir a row "Preferências" do Perfil por "Idioma"**

Em `src/app/(tabs)/perfil.tsx`, linha 123, substituir:

```tsx
<MenuRow icon="settings" label="Preferências" onPress={() => Alert.alert('Preferências', 'Funcionalidade em breve.')} />
```

por:

```tsx
<MenuRow icon="globe" label={t('language.title')} onPress={() => router.push('/idioma')} />
```

E no topo da função `PerfilScreen` adicionar `const { t } = useTranslation();` (import de `react-i18next`).

- [ ] **Step 3: Verificação + commit**

Run: `npx expo start` (para regenerar typed routes com `/idioma`)
Run: `npx tsc --noEmit` (esperado: limpo)
Run: `npx expo lint` (esperado: 0 erros)
```bash
git add src/app/idioma.tsx "src/app/(tabs)/perfil.tsx" src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): modal de seleção de idioma + entrada no Perfil"
```

---

### Task 6: Componentes partilhados — batch 1 (simples)

**Files:**
- Modify: `src/components/DashboardHeader.tsx`, `PerformanceCard.tsx`, `NextBoomCard.tsx`, `BoomHourCard.tsx`, `AlarmToggle.tsx`, `AudioRecorder.tsx`, `AiFab.tsx`, `ui.tsx`, `PlanCard.tsx`, `PremiumLock.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

Regra: em cada componente, adicionar `const { t } = useTranslation();` (import `react-i18next`) e substituir as strings. Chaves a adicionar ao `pt.json` (uma única adição nesta task):

```json
{
  "components": {
    "dashboardHeader": {
      "morning": "Bom dia",
      "afternoon": "Boa tarde",
      "evening": "Boa noite",
      "appName": "The Magic Trader",
      "pro": "PRO"
    },
    "performanceCard": {
      "title": "DESEMPENHO",
      "viewHistory": "Ver histórico ›",
      "winRate": "Taxa de acerto",
      "tp": "TP {{pct}}%",
      "sl": "SL {{pct}}%",
      "totalPips": "Pips totais",
      "totalSignals": "Sinais totais",
      "activeNow": "Ativos agora"
    },
    "nextBoomCard": {
      "calculating": "O próximo BOOM está sendo calculado…",
      "title": "PRÓXIMO BOOM",
      "boom": "BOOM",
      "launchAt": "Lançamento às {{time}}"
    },
    "boomHourCard": {
      "live": "● AO VIVO",
      "upcoming": "PRÓXIMO",
      "closed": "ENCERRADO",
      "wat": "WAT",
      "gmt": "GMT {{h}}",
      "window": "JANELA EM CURSO",
      "forEntry": "para a entrada",
      "volatility": "VOLATILIDADE",
      "closedHint": "Encerrado · o ciclo recomeça amanhã"
    },
    "alarmToggle": {
      "active": "Alarme ativo",
      "activate": "Ativar alarme",
      "hint5min": "Notificamos-te 5 min antes.",
      "blocked": "Notificações bloqueadas — ativa nas definições do sistema."
    },
    "audioRecorder": {
      "stop": "PARAR · {{seconds}}s",
      "audio": "ÁUDIO"
    },
    "aiFab": {
      "label": "Suporte por IA"
    },
    "ui": {
      "lockedPrefix": "🔒 "
    },
    "planCard": {
      "active": "ATIVO",
      "perMonth": "/mês"
    },
    "premiumLock": {
      "viewPlans": "Ver Planos"
    }
  }
}
```

- [ ] **Step 1: `DashboardHeader.tsx`**

Adicionar `useTranslation`. Saudação: substituir `'Bom dia'/'Boa tarde'/'Boa noite'` por `t('components.dashboardHeader.morning'|'afternoon'|'evening')` (mesmo condicional). Fallback `'Trader'` → `t('common.trader')`. `'The Magic Trader'` → `t('components.dashboardHeader.appName')`. Badge `'PRO'` → `t('components.dashboardHeader.pro')`.

- [ ] **Step 2: `PerformanceCard.tsx`**

`'DESEMPENHO'` → `t('components.performanceCard.title')`; `'Ver histórico ›'` → `viewHistory`; `'Taxa de acerto'` → `winRate`; `TP {pct}%`/`SL {pct}%` → `t('components.performanceCard.tp', { pct })` / `sl`; `'Pips totais'`/`'Sinais totais'`/`'Ativos agora'` → respetivas chaves.

- [ ] **Step 3: `NextBoomCard.tsx`**

`'O próximo BOOM está sendo calculado…'` → `calculating`; `'PRÓXIMO BOOM'` → `title`; badge `'BOOM'` → `boom`; `'Lançamento às HH:MM'` → `t('components.nextBoomCard.launchAt', { time: hhmm })`.

- [ ] **Step 4: `BoomHourCard.tsx`**

`'● AO VIVO'`/`'PRÓXIMO'`/`'ENCERRADO'` → `live`/`upcoming`/`closed`; `'WAT'` → `wat`; `GMT {h}` → `t('components.boomHourCard.gmt', { h })`; `'JANELA EM CURSO'` → `window`; `'para a entrada'` → `forEntry`; `'VOLATILIDADE'` → `volatility`; `'Encerrado · o ciclo recomeça amanhã'` → `closedHint`.

- [ ] **Step 5: `AlarmToggle.tsx`**

`'Alarme ativo'`/`'Ativar alarme'` → `active`/`activate`; `'Notificamos-te 5 min antes.'` → `hint5min`; `'Notificações bloqueadas — ativa nas definições do sistema.'` → `blocked`.

- [ ] **Step 6: `AudioRecorder.tsx`, `AiFab.tsx`, `ui.tsx`, `PlanCard.tsx`, `PremiumLock.tsx`**

`AudioRecorder`: `'PARAR · Ns'` → `t('components.audioRecorder.stop', { seconds })`; `'ÁUDIO'` → `audio`.
`AiFab`: `accessibilityLabel="Suporte por IA"` → `accessibilityLabel={t('components.aiFab.label')}`.
`ui.tsx` linha 174: `🔒 ${label}` → `` {`${t('components.ui.lockedPrefix')}${label}`} `` (a `t` vem de `useTranslation()` já presente no componente Chip, ou adicionar).
`PlanCard`: `'ATIVO'` → `t('components.planCard.active')`; `'/mês'` → `t('components.planCard.perMonth')`.
`PremiumLock`: `'Ver Planos'` → `t('components.premiumLock.viewPlans')`.

- [ ] **Step 7: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add src/components src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): componentes partilhados batch 1 (headers, cards, toggles, ui)"
```

---

### Task 7: Componentes partilhados — batch 2 (SignalCard, BoomCard, WalletCard, LegalDoc, TradingViewChart)

**Files:**
- Modify: `src/components/SignalCard.tsx`, `BoomCard.tsx`, `WalletCard.tsx`, `LegalDoc.tsx`, `TradingViewChart.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "components": {
    "signalCard": {
      "waiting": "AGUARDAR",
      "active": "ATIVO",
      "pending": "PENDENTE",
      "entry": "ENTRADA",
      "pips": "{{count}} pips",
      "now": "agora",
      "rr": "RR {{value}}:",
      "inProfit": "Em lucro",
      "inLoss": "Em perda",
      "atPoint": "No ponto",
      "analysis": "ANÁLISE",
      "moreReasons": "+{{count}} razões"
    },
    "boomCard": {
      "live": "🚨 AO VIVO",
      "upcoming": "⏳ PRÓXIMO",
      "closed": "✓ ENCERRADO",
      "trader": "Trader",
      "pro": "PRO",
      "buy": "BUY {{pct}}%",
      "sell": "SELL {{pct}}%",
      "votes": "{{total}} votos · {{pct}}% BUY",
      "comments": "Comentários ({{count}})",
      "noComments": "Ainda não há comentários. Sê o primeiro!",
      "commentPlaceholder": "Escreve um comentário…",
      "audioAttached": "Áudio anexado ✕",
      "send": "Enviar",
      "loginToComment": "Inicia sessão para comentar."
    },
    "walletCard": {
      "balance": "SALDO DISPONÍVEL",
      "capitalBase": "Capital base · ",
      "targetReturn": "Meta de retorno · ",
      "capital": "Capital",
      "target": "Meta",
      "riskPerOp": "Risco/op",
      "movements": "MOVIMENTAÇÕES",
      "initialDeposit": "Depósito inicial",
      "today": "Hoje",
      "yesterday": "Ontem",
      "daysAgo": "Há {{count}} dias",
      "totalBalance": "Saldo total",
      "wallet": "Carteira"
    },
    "legalDoc": {
      "lastUpdated": "Última atualização: {{date}}",
      "january2026": "Janeiro 2026"
    },
    "tradingViewChart": {
      "entry": "ENTRADA",
      "stopLoss": "SL",
      "takeProfit": "TP"
    }
  }
}
```

- [ ] **Step 1: `SignalCard.tsx`**

`'AGUARDAR'` → `t('components.signalCard.waiting')`; `'ATIVO'` → `active`; `'PENDENTE'` → `pending`; `'ENTRADA'` (2 ocorrências) → `entry`; `'{n} pips'` → `t('components.signalCard.pips', { count: n })`; `'agora'` → `now`; `'RR 1:'` → `t('components.signalCard.rr', { value: 1 })`; `'Em lucro'/'Em perda'/'No ponto'` → `inProfit`/`inLoss`/`atPoint`; `'ANÁLISE'` → `analysis`; `'+{n} razões'` → `t('components.signalCard.moreReasons', { count: n })`. O conteúdo de `reasons` vem da BD (não traduzir).

- [ ] **Step 2: `BoomCard.tsx`**

`'🚨 AO VIVO'`/`'⏳ PRÓXIMO'`/`'✓ ENCERRADO'` → `live`/`upcoming`/`closed`; fallback `'Trader'` → `t('components.boomCard.trader')`; `'PRO'` → `pro`; `'BUY {pct}%'`/`'SELL {pct}%'` → `t('components.boomCard.buy', { pct })`/`sell`; `'{total} votos · {pct}% BUY'` → `t('components.boomCard.votes', { total, pct })`; `'Comentários (n)'` → `t('components.boomCard.comments', { count: n })`; `'Ainda não há comentários…'` → `noComments`; placeholder `'Escreve um comentário…'` → `commentPlaceholder`; `'Áudio anexado ✕'` → `audioAttached`; `'Enviar'` → `send`; `'Inicia sessão para comentar.'` → `loginToComment`.

- [ ] **Step 3: `WalletCard.tsx`**

Substituir os labels e os **dados mock de movimentações** (depósito inicial/Hoje/Sinal TP/Ontem/Sinal SL/Há 2 dias/Saldo total/Carteira) pelas chaves correspondentes, interpolando símbolos onde aplicável (ex.: `Sinal TP · XAU/USD` → `Sinal TP · {par}` — criar chave `walletCard.signalTp`: `"Sinal TP · {{par}}"`). Adicionar ao JSON:

```json
{ "components": { "walletCard": {
  "signalTp": "Sinal TP · {{par}}",
  "signalSl": "Sinal SL · {{par}}"
} } }
```

- [ ] **Step 4: `LegalDoc.tsx`**

`'Última atualização:'` → `t('components.legalDoc.lastUpdated', { date })`; default `'Janeiro 2026'` → `t('components.legalDoc.january2026')`.

- [ ] **Step 5: `TradingViewChart.tsx`**

- `locale: "pt"` (HTML do widget) → `locale: i18n.language` (import `{ i18n }` de `@/lib/i18n`). Se o widget suportar só códigos específicos, mapear: `{ en: 'en', es: 'es', de: 'de', fr: 'fr', it: 'it', nl: 'nl', pt: 'pt', ru: 'ru', zh: 'zh', ja: 'ja', ko: 'ko', ar: 'ar' }` com fallback `'en'`.
- Labels `ENTRADA`/`SL`/`TP` injetados → `t('components.tradingViewChart.entry'|'stopLoss'|'takeProfit')`.

- [ ] **Step 6: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add src/components src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): componentes batch 2 (signal, boom, wallet, legal, gráfico)"
```

---

### Task 8: Ecrãs das tabs — layout + início + análises

**Files:**
- Modify: `src/app/(tabs)/_layout.tsx`, `src/app/(tabs)/inicio.tsx`, `src/app/(tabs)/analises.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "tabs": {
    "inicio": "Início",
    "analises": "Análises",
    "historico": "Histórico",
    "horarios": "Horários",
    "comunidade": "Comunidade",
    "perfil": "Perfil",
    "planos": "PLANOS"
  },
  "inicio": {
    "nextHours": "Próximos horários",
    "highVolatility": "Janelas de alta volatilidade",
    "viewAll": "Ver todos",
    "loadingHours": "A carregar horários…",
    "emptyTitle": "Sem horários",
    "emptyBody": "Ainda não há horários ativos.",
    "wat": "WAT"
  },
  "analises": {
    "title": "Análises",
    "premiumTitle": "{{what}} é Premium",
    "premiumSignals": "Sinais {{what}} são Premium",
    "premiumDesc": "Desbloqueia todos os pares e timeframes com o plano PRO.",
    "currentPrice": "Preço atual",
    "pipsOpen": "Pips abertos",
    "entry": "ENTRADA",
    "sl": "SL ({{count}} pips)",
    "tp": "TP ({{count}} pips)",
    "viewFull": "Ver análise completa ›",
    "signalsCount": "{{count}} SINAIS … toca para ver no gráfico",
    "highlight": "DESTAQUE",
    "loading": "A carregar sinais…",
    "emptyTitle": "Sem sinais",
    "emptyBody": "Ainda não há sinais disponíveis.",
    "filterAll": "Todos"
  }
}
```

Nota: `TIMEFRAMES = ['Todos','M15','H1','H4']` e `SIGNAL_TYPES = ['Todos','BUY','SELL','AGUARDAR']` de `src/core/gating.ts` **mantêm-se como dados** (os valores `'BUY'/'SELL'/'AGUARDAR'` são tipo de sinal). No render dos chips, substituir o literal `'Todos'` por `t('analises.filterAll')`. O `'AGUARDAR'` do gating permanece como valor; a tradução visível acontece no SignalCard (Task 7, chave `signalCard.waiting`).

- [ ] **Step 1: `src/app/(tabs)/_layout.tsx`**

Adicionar `const { t } = useTranslation();`. Títulos de tabs: `title: t('tabs.inicio')` etc. para as 6 tabs. Botão `'PLANOS'` → `t('tabs.planos')`.

- [ ] **Step 2: `src/app/(tabs)/inicio.tsx`**

Adicionar `useTranslation`. `TICKER_PAIRS` mantém-se. `'Próximos horários'` → `t('inicio.nextHours')`; `'Janelas de alta volatilidade'` → `highVolatility`; `'Ver todos'` → `viewAll`; spinner `'A carregar horários…'` → `loadingHours`; EmptyState `'Sem horários'`/`'Ainda não há horários ativos.'` → `emptyTitle`/`emptyBody`; `'WAT'` → `wat`.

- [ ] **Step 3: `src/app/(tabs)/analises.tsx`**

Adicionar `useTranslation`. Substituir todas as strings (ver mapa da Task 1 research: `:96` título, `:131-132` PremiumLock, `:144` DESTAQUE, `:159-168` MetricGrid, `:174-182` ENTRADA/SL/TP, `:188` ver análise, `:195` contador, `:200-204` spinner/empty). Usar as chaves `analises.*` acima, interpolando `{{count}}`/`{{what}}` nos sítios assinalados. Nos chips de filtro, `'Todos'` → `t('analises.filterAll')`.

- [ ] **Step 4: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add "src/app/(tabs)/_layout.tsx" "src/app/(tabs)/inicio.tsx" "src/app/(tabs)/analises.tsx" src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): tabs layout + início + análises"
```

---

### Task 9: Ecrãs historico + horarios

**Files:**
- Modify: `src/app/(tabs)/historico.tsx`, `src/app/(tabs)/horarios.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "historico": {
    "allPairs": "Todos os pares",
    "totalSignals": "Total Sinais",
    "winRate": "Taxa de Acerto",
    "totalPips": "Pips Totais",
    "closedSignals": "SINAIS FECHADOS",
    "entry": "Entrada",
    "stopLoss": "Stop Loss",
    "takeProfit": "Take Profit",
    "rr": "R:R",
    "result": "Resultado",
    "dateTime": "Data/Hora (WAT)",
    "tpHit": "✅ TAKE PROFIT",
    "slHit": "❌ STOP LOSS",
    "loading": "A carregar histórico…",
    "emptyTitle": "Sem sinais fechados",
    "emptyBody": "Os sinais fechados aparecem aqui."
  },
  "horarios": {
    "watAngola": "WAT · ANGOLA",
    "gmt": "GMT",
    "nextBoom": "PRÓXIMO BOOM",
    "forEntry": "para a entrada",
    "title": "Hora do Boom",
    "watUtc": "WAT (UTC-1)",
    "description": "Janelas de alta volatilidade. Ativa alarmes para não perderes a entrada.",
    "filtersActive": "Filtros ativos · {{count}} boom(s) oculto(s)",
    "loading": "A carregar horários…",
    "emptyTitle": "Sem horários",
    "emptyBody": "Ainda não há horários ativos.",
    "adjustFilters": "Ajustar filtros",
    "live": "AO VIVO",
    "upcoming": "PRÓXIMOS",
    "closed": "ENCERRADOS"
  }
}
```

- [ ] **Step 1: `historico.tsx`**

Adicionar `useTranslation`. `'Todos os pares'` (default + opção) → `t('historico.allPairs')`; stats `:90-92` → `totalSignals`/`winRate`/`totalPips`; `'SINAIS FECHADOS'` → `closedSignals`; detalhes `:58-78` (`Entrada`/`Stop Loss`/`Take Profit`/`R:R`/`Resultado`/`Data/Hora (WAT)`) → chaves respetivas; `'✅ TAKE PROFIT'`/`'❌ STOP LOSS'` → `tpHit`/`slHit`; spinner `:125` → `loading`; EmptyState `:127` → `emptyTitle`/`emptyBody`.

- [ ] **Step 2: `horarios.tsx`**

Adicionar `useTranslation`. `'WAT · ANGOLA'`/`'GMT'` → `watAngola`/`gmt`; `'PRÓXIMO BOOM'` → `nextBoom`; `'para a entrada'` → `forEntry`; `'Hora do Boom'` → `title`; `'WAT (UTC-1)'` → `watUtc`; descrição `:147` → `description`; `'Filtros ativos · N boom(s) oculto(s)'` → `t('horarios.filtersActive', { count: n })`; spinner `:161` → `loading`; EmptyStates `:163-170` → `emptyTitle`/`emptyBody`; `'Ajustar filtros'` → `adjustFilters`; `'AO VIVO'`/`'PRÓXIMOS'`/`'ENCERRADOS'` → `live`/`upcoming`/`closed`.

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add "src/app/(tabs)/historico.tsx" "src/app/(tabs)/horarios.tsx" src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): histórico e horários"
```

---

### Task 10: Ecrãs comunidade + perfil

**Files:**
- Modify: `src/app/(tabs)/comunidade.tsx`, `src/app/(tabs)/perfil.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "comunidade": {
    "all": "Todos",
    "live": "Ao Vivo",
    "upcoming": "Próximos",
    "closed": "Encerrados",
    "liveBadge": "AO VIVO",
    "title": "Boom Times",
    "loading": "A carregar comunidade…",
    "emptyTitle": "Sem booms",
    "emptyBody": "Ainda não há booms para mostrar."
  },
  "perfil": {
    "fallbackAvatar": "T",
    "trader": "Trader",
    "verified": "Conta verificada",
    "notAuthed": "Não autenticado",
    "premiumUntil": "Premium até {{date}}",
    "premium": "Premium",
    "freePlan": "Plano Grátis",
    "walletSection": "Carteira",
    "bancaManagement": "Gestão de banca",
    "availablePremium": "Disponível no plano Premium",
    "viewBanca": "Ver banca ›",
    "walletLockTitle": "Carteira & Gestão de Banca",
    "walletLockDesc": "Controla capital, metas e risco por operação.",
    "sectionAccount": "CONTA",
    "mySubscription": "Minha Assinatura",
    "growthPlan": "Plano de Crescimento",
    "banca": "Gestão de Banca",
    "sectionPreferences": "PREFERÊNCIAS",
    "notifications": "Notificações",
    "manageAlerts": "Gerenciar Alertas",
    "security": "Segurança",
    "sectionWithdraw": "SAQUE E DEPÓSITO",
    "binance": "Binance",
    "express": "Express",
    "signOut": "Sair",
    "signIn": "Entrar",
    "version": "The Magic Trader · v1.0.0",
    "signOutTitle": "Sair",
    "signOutBody": "Tens a certeza que queres sair?"
  }
}
```

- [ ] **Step 1: `comunidade.tsx`**

Adicionar `useTranslation`. `FILTERS` (labels `Todos`/`Ao Vivo`/`Próximos`/`Encerrados`) → `t('comunidade.all'|'live'|'upcoming'|'closed')` (manter a estrutura do array com chaves por item); badge `'AO VIVO'` → `liveBadge`; `'Boom Times'` → `title`; spinner `'A carregar comunidade…'` → `loading`; EmptyState → `emptyTitle`/`emptyBody`.

- [ ] **Step 2: `perfil.tsx`**

Adicionar `useTranslation` (já no Task 5). Substituir todas as strings pelo mapa acima. `periodEnd` passa a `new Date(...).toLocaleDateString(i18n.language)` (importar `i18n` de `@/lib/i18n`). Alertas `'Funcionalidade em breve.'` → `t('common.comingSoon')` (nas rows Segurança). Confirm logout → `t('perfil.signOutTitle')`/`t('perfil.signOutBody')`, botões `'Cancelar'` → `t('common.cancel')`, `'Sair'` → `t('perfil.signOut')`.

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add "src/app/(tabs)/comunidade.tsx" "src/app/(tabs)/perfil.tsx" src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): comunidade e perfil"
```

---

### Task 11: Ecrã planos

**Files:**
- Modify: `src/app/planos.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

⚠️ `planos.tsx` foi editado pelo workstream paralelo (const `PLANS` corrigida). **Reler o ficheiro antes de editar.** O ficheiro tem ~643 linhas com ~60 strings.

Estratégia: extrair constantes PT para chaves e usar `t()` no render. Chaves a adicionar ao `pt.json`:

```json
{
  "planos": {
    "title": "Planos",
    "heroTitle": "Escolhe o teu plano",
    "heroSubtitle": "Começa grátis ou desbloqueia análises completas com um plano pago.",
    "premiumUntil": "Premium ativo até {{date}}",
    "currentFree": "Plano atual: Grátis",
    "usd": "USD ($)",
    "aoa": "AOA (Kz)",
    "switchTo": "Mudar para {{plan}}",
    "subscribe": "Assinar {{plan}}",
    "createAndSubscribe": "Criar conta e assinar {{plan}}",
    "free": "Gratuito",
    "freeTagline": "Para experimentar",
    "currentPlan": "Plano Atual",
    "premiumAlerts": "Alertas Premium",
    "featured": "TOP",
    "mostPopular": "MAIS POPULAR",
    "trustSecure": "Pagamento seguro",
    "trustCancel": "Cancela quando quiseres",
    "trustSupport": "Suporte dedicado",
    "alertsWhatsapp": "WhatsApp",
    "alertsWhatsappDesc": "Notificações instantâneas",
    "alertsTelegram": "Telegram",
    "alertsTelegramDesc": "Bot dedicado",
    "alertsEmail": "Email",
    "alertsEmailDesc": "Resumo diário",
    "paymentTitle": "Pagamento: {{plan}}",
    "paymentAmount": "Valor: {{amount}}/mês",
    "chooseMethod": "Escolha o método:",
    "change": "Alterar",
    "payInstructions": "Pague para o número/UID acima",
    "afterPayment": "Após o pagamento, envie o comprovante…",
    "cancel": "Cancelar",
    "receiptSent": "Já enviei o comprovativo",
    "receiptOk": "Comprovante recebido",
    "receiptMsg": "Obrigado! O seu comprovante foi enviado para revisão."
  }
}
```

- [ ] **Step 1: Extrair constantes de dados para chaves**

`PLANS` (taglines, badges `MAIS POPULAR`/`TOP`) → usar chaves `planos.*` no render (ex.: `tagline: t('planos.free')` se a `tagline` for "Para começar a sério", criar chaves `taglineBasic`/`taglinePro`/`taglinePremium`). Adicionar ao JSON:

```json
{ "planos": {
  "taglineBasic": "Para começar a sério",
  "taglinePro": "Para traders a sério",
  "taglinePremium": "Experiência completa"
} }
```

`FREE_FEATURES`/`BASIC_FEATURES`/`PRO_FEATURES`/`PREMIUM_FEATURES` (28 strings) → cada texto passa a `t('planos.feature.{n}')` **ou** manter os textos como `PlanFeature` com chave i18n; escolher a variante mais simples que evite alterar `PlanCard` (que recebe `features` por props): converter o conteúdo das arrays em **chaves de tradução** e chamar `t()` no `features` quando passado ao `PlanCard`. Adicionar chaves `planos.featureBasic.[0..n]` etc. com os textos exatos atuais.

- [ ] **Step 2: Render + PaymentModal**

Substituir todas as strings do render (hero, badges, segmento USD/AOA, botões, cards) e do `PaymentModal` pelas chaves `planos.*`, interpolando `{{plan}}`/`{{amount}}`/`{{date}}`. Alerts `'Comprovante recebido'`/`'Obrigado!…'` → `receiptOk`/`receiptMsg`. Corrigir também a duplicação `handlePaymentConfirmed` (bug pré-existente) se for trivial (manter comportamento igual).

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (0 erros)
```bash
git add src/app/planos.tsx src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): ecrã de planos e modal de pagamento"
```

---

### Task 12: Ecrãs banca + definicoes-booms + notificacoes

**Files:**
- Modify: `src/app/banca.tsx`, `src/app/definicoes-booms.tsx`, `src/app/notificacoes.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "banca": {
    "title": "Gestão de Banca",
    "lockTitle": "Gestão de Banca Premium",
    "lockDesc": "Capital, metas e risco por operação. Controla tudo numa só carteira.",
    "invalidValue": "Valor inválido",
    "positiveCapital": "O capital tem de ser um número positivo.",
    "loading": "A carregar…",
    "subtitle": "Controla o teu capital, metas e risco por operação.",
    "totalCapital": "CAPITAL TOTAL",
    "targetReturn": "Meta de Retorno",
    "inMonths": "em {{count}} meses",
    "riskPerOp": "Risco por Operação",
    "recommended": "RECOMENDADO",
    "performance": "Desempenho",
    "save": "Guardar",
    "edit": "Editar",
    "inputCapital": "CAPITAL (USD)",
    "inputCurrent": "VALOR ATUAL (USD)",
    "inputTarget": "META (%)",
    "inputRisk": "RISCO POR OPERAÇÃO (%)",
    "weekly": "Semanal",
    "monthly": "Mensal",
    "total": "Total"
  },
  "definicoesBooms": {
    "title": "Definições do Boom",
    "enableNotification": "Ativa as notificações nas definições do teu telemóvel.",
    "alarmsTitle": "Alarmes & Notificações",
    "alarmsDesc": "Recebe avisos 5 minutos antes de cada janela de alta volatilidade.",
    "enable": "Ativar",
    "lockTitle": "Personalização Premium",
    "lockDesc": "Filtra os booms por tendência, pares e volatilidade.",
    "visibleCount": "{{visible}} de {{total}} booms visíveis no teu horário.",
    "trend": "Tendência",
    "windowsByVol": "Janelas por volatilidade",
    "volHint": "Só vês janelas dentro do nível de volatilidade escolhido.",
    "pairs": "Pares",
    "pairsHint": "Só vês booms com estes pares",
    "all": "Todos",
    "noPairs": "Nenhum par associado aos booms.",
    "filterBooms": "Filtrar booms",
    "filterDesc": "Oculta janelas que não queres ver",
    "reset": "Repor predefinições",
    "volAll": "Todas",
    "volHigh": "Alta",
    "volMedium": "Média",
    "volLow": "Baixa"
  },
  "notificacoes": {
    "title": "Notificações",
    "soon": "Em breve",
    "delivered": "Entregue",
    "inMin": "Em {{count}} min",
    "inHm": "Em {{h}}h{{min}}min",
    "loading": "A carregar…",
    "emptyTitle": "Sem notificações",
    "emptyBody": "Os alarmes da Hora do Boom aparecem aqui quando os ativares.",
    "enableAlarms": "Ativar alarmes",
    "manageAlerts": "Gerenciar Alertas",
    "clearAll": "Limpar tudo"
  }
}
```

- [ ] **Step 1: `banca.tsx`** — Adicionar `useTranslation`; substituir strings (Alertas de validação incluídos), interpolar `{{count}}` em `inMonths`.

- [ ] **Step 2: `definicoes-booms.tsx`** — `VOL_TIERS` labels vêm de `boomPrefs.ts`; substituir o **render** (`definicoes-booms.tsx` recebe `VOL_TIERS`) usando `t('definicoesBooms.volAll'|'volHigh'|'volMedium'|'volLow')` no mapa do array no sítio de uso, e manter `VOL_TIERS` como dados (labels `Todas`/`Alta`/… permanecem como chave canónica). Substituir as demais strings pelas chaves `definicoesBooms.*` (interpolar `{{visible}}`/`{{total}}`).

- [ ] **Step 3: `notificacoes.tsx`** — `formatFire` (`'Em breve'`/`'Entregue'`/`'Em N min'`/`'Em Nh{N}min'`) → `t('notificacoes.soon'|'delivered'|'inMin'|'inHm', { count })`; restantes strings pelas chaves.

- [ ] **Step 4: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add src/app/banca.tsx src/app/definicoes-booms.tsx src/app/notificacoes.tsx src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): banca, definições do boom e notificações"
```

---

### Task 13: Ecrãs sinal/[id] + plano-crescimento + suporte-ia (+ aiSupport)

**Files:**
- Modify: `src/app/sinal/[id].tsx`, `src/app/plano-crescimento.tsx`, `src/app/suporte-ia.tsx`
- Modify: `src/lib/aiSupport.ts`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "sinal": {
    "title": "Análise Completa",
    "loading": "A carregar análise…",
    "notFound": "Sinal não encontrado",
    "shareText": "Entrada: {{entry}}\nSL: {{sl}}\nTP: {{tp}}\nConfiança: {{confidence}}\nThe Magic Trader",
    "slPips": "SL PIPS",
    "tpPips": "TP PIPS",
    "rr": "RR",
    "technical": "ANÁLISE TÉCNICA",
    "noReasons": "Sem justificações",
    "riskWarning": "Aviso de risco: esta análise tem fins educacionais…"
  },
  "planoCrescimento": {
    "title": "Plano de Crescimento",
    "select": "Selecione seu plano",
    "conservative": "Conservador",
    "conservativeDesc": "+20% em 3 meses · Menor risco, consistência",
    "balanced": "Equilibrado",
    "aggressive": "Agressivo",
    "projection": "Projeção",
    "investment": "Investimento",
    "estimatedReturn": "Retorno Estimado",
    "period": "Período",
    "months": "{{count}} meses",
    "estimatedProfit": "Lucro estimado: +{{value}} USD",
    "planActive": "✓ Plano Ativo",
    "activate": "Ativar Plano"
  },
  "suporteIa": {
    "title": "Suporte IA",
    "subtitle": "Assistente The Magic Trader",
    "close": "Fechar suporte",
    "clear": "Limpar conversa",
    "send": "Enviar",
    "welcome": "Olá, sou o assistente do TMT 👋",
    "welcomeDesc": "Tiro dúvidas sobre sinais, planos, alertas, banca e a app.",
    "placeholder": "Escreve a tua pergunta…",
    "suggestion1": "Como funcionam os planos?",
    "suggestion2": "O que é a Hora do Boom?",
    "suggestion3": "Como ativo os alarmes?",
    "suggestion4": "Como gero a minha banca?"
  },
  "aiErrors": {
    "contact": "Erro ao contactar o suporte. Tenta novamente.",
    "noResponse": "A IA não devolveu uma resposta.",
    "connection": "Erro de ligação. Verifica a tua internet e tenta novamente."
  }
}
```

- [ ] **Step 1: `sinal/[id].tsx`** — Adicionar `useTranslation`; substituir todas as strings (share text via `t('sinal.shareText', { entry, sl, tp, confidence })`, aviso de risco, etc.).

- [ ] **Step 2: `plano-crescimento.tsx`** — `PLANS` (descrições) → chaves; substituir restantes.

- [ ] **Step 3: `suporte-ia.tsx`** — `SUGGESTIONS` → 4 chaves; `accessibilityLabel`s → `close`/`clear`/`send`; título/subtítulo/placeholder → chaves.

- [ ] **Step 4: `aiSupport.ts`** — `import { i18n } from '@/lib/i18n'`; erros `:31,35,39` → `i18n.t('aiErrors.contact'|'noResponse'|'connection')`.

- [ ] **Step 5: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add "src/app/sinal/[id].tsx" src/app/plano-crescimento.tsx src/app/suporte-ia.tsx src/lib/aiSupport.ts src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): análise completa, plano de crescimento e suporte IA"
```

---

### Task 14: Ecrãs de auth (login, whatsapp) + erros de auth

**Files:**
- Modify: `src/app/(auth)/login.tsx`, `src/app/(auth)/whatsapp.tsx`
- Modify: `src/lib/googleAuth.ts`, `src/lib/whatsappAuth.ts`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "auth": {
    "tagline": "Análise inteligente. Entradas estratégicas.",
    "continueGoogle": "Continuar com Google",
    "enterWhatsapp": "Entrar com WhatsApp",
    "legalFooter": "Ao continuar aceitas os nossos Termos e a nossa Política de Privacidade.",
    "whatsappTestMode": "Modo de teste — introduz o número e entra de imediato, sem código.",
    "testBadge": "MODO TESTE · login imediato sem SMS",
    "phoneLabel": "NÚMERO DE WHATSAPP",
    "phonePlaceholder": "+244 9xx xxx xxx",
    "invalidPhone": "Introduz um número de WhatsApp válido com o código do país.",
    "enterApp": "Entrar na app"
  },
  "authErrors": {
    "googleStart": "Não foi possível iniciar o login com o Google.",
    "googleNotComplete": "O login com o Google não foi concluído.",
    "googleError": "Ocorreu um erro ao iniciar sessão com o Google.",
    "whatsappUnknown": "Erro desconhecido ao iniciar sessão.",
    "whatsappError": "Ocorreu um erro ao iniciar sessão.",
    "whatsappInvalidCode": "Código inválido. Tenta novamente."
  }
}
```

- [ ] **Step 1: `login.tsx` e `whatsapp.tsx`** — Adicionar `useTranslation`; substituir strings pelas chaves `auth.*`.

- [ ] **Step 2: `googleAuth.ts` e `whatsappAuth.ts`** — `import { i18n }`; erros → `i18n.t('authErrors.*')`.

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add "src/app/(auth)" src/lib/googleAuth.ts src/lib/whatsappAuth.ts src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): autenticação (login, whatsapp) e erros"
```

---

### Task 15: Documentos legais (termos, privacidade, aviso de risco)

**Files:**
- Modify: `src/app/termos.tsx`, `src/app/privacidade.tsx`, `src/app/aviso-risco.tsx`
- Modify: `src/lib/i18n/locales/pt.json`

⚠️ Estes documentos contêm parágrafos legais inteiros. Nesta fase, os **títulos de secção** e **títulos** são traduzidos; os **corpos de texto** são mantidos em PT (conteúdo jurídico não gera tradução fiável em 14 idiomas — documentado no spec como "conteúdo do servidor não traduzido"). `LegalDoc` (Task 7) já traduz "Última atualização".

- [ ] **Step 1: Adicionar chaves de títulos ao `pt.json`**

```json
{
  "legal": {
    "termosTitle": "Termos de Uso",
    "privacidadeTitle": "Política de Privacidade",
    "avisoRiscoTitle": "Aviso de Risco"
  }
}
```

- [ ] **Step 2: Substituir os títulos nos 3 ecrãs** por `t('legal.termosTitle'|'privacidadeTitle'|'avisoRiscoTitle')`. Corpos legais **mantêm-se como estão** (PT), com comentário curto no ficheiro: `// Legal body: PT only (conteúdo jurídico não traduzido)`.

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add src/app/termos.tsx src/app/privacidade.tsx src/app/aviso-risco.tsx src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): títulos dos documentos legais (corpos mantêm-se PT)"
```

---

### Task 16: Ecrã admin

**Files:**
- Modify: `src/app/(tabs)/admin.tsx`
- Modify: `src/lib/adminApi.ts`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "admin": {
    "dashboard": "Dashboard",
    "signals": "Sinais",
    "boomHours": "Boom Hours",
    "boomTimes": "Boom Times",
    "posts": "Posts",
    "users": "Usuários",
    "checkingAccess": "A verificar acesso…",
    "loading": "A carregar admin…",
    "restricted": "Acesso restrito",
    "restrictedDesc": "Esta área é exclusiva para administradores.",
    "done": "Concluído",
    "error": "Erro",
    "errorGeneric": "Ocorreu um erro.",
    "usersCard": "Usuários",
    "signalsToday": "Sinais Hoje",
    "winRate": "Taxa de Acerto",
    "premium": "Premium",
    "generateSignals": "Gerar Sinais",
    "closeTpSl": "Fechar TP/SL",
    "clearRegen": "Limpar e Regenerar",
    "newSignal": "NOVO SINAL",
    "labelPar": "PAR",
    "labelType": "TIPO",
    "labelConfidence": "CONFIANÇA %",
    "labelEntry": "ENTRADA",
    "labelSl": "SL",
    "labelTp": "TP",
    "labelReasons": "RAZÕES",
    "addSignal": "Adicionar Sinal",
    "signalsList": "SINAIS ({{count}})",
    "noSignals": "Sem sinais",
    "deleteConfirm": "Apagar este sinal?",
    "newHour": "NOVO HORÁRIO",
    "newBoomTime": "NOVO BOOM TIME",
    "newPost": "NOVO POST",
    "usersList": "USUÁRIOS ({{count}}) · {{premium}} premium",
    "london": "Boom de Londres",
    "pairsPlaceholder": "EURUSD, GBPUSD",
    "disabled": "Desativado",
    "published": "Publicado",
    "registered": "Registo: {{date}}"
  },
  "adminErrors": {
    "notAuthed": "Não autenticado",
    "unknown": "Erro desconhecido",
    "generate": "Erro ao gerar sinais",
    "close": "Erro ao fechar sinais"
  }
}
```

- [ ] **Step 1: `admin.tsx`** — Adicionar `useTranslation`; substituir `TABS` labels e todas as strings do painel (6 sub-tabs, cards, botões, inputs, placeholders, confirms) pelas chaves, interpolando `{{count}}`/`{{premium}}`/`{{date}}`. Os `toLocaleString('pt-PT')`/`toLocaleString()` → `toLocaleString(i18n.language)` (importar `i18n`).

- [ ] **Step 2: `adminApi.ts`** — `import { i18n }`; erros `:8,21,113,130,148` → `i18n.t('adminErrors.*')`.

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add "src/app/(tabs)/admin.tsx" src/lib/adminApi.ts src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): painel admin"
```

---

### Task 17: Notificações (channels + conteúdo) e boomPrefs

**Files:**
- Modify: `src/lib/notifications.ts`, `src/lib/signalNotifications.ts`
- Modify: `src/lib/i18n/locales/pt.json`

Chaves a adicionar ao `pt.json`:

```json
{
  "notifications": {
    "channelBoomWarn": "Avisos da Hora do Boom (5 min)",
    "channelBoomAlarm": "Alarme da Hora do Boom (exato)",
    "channelPlans": "Planos e Ofertas",
    "channelSignals": "Novos Sinais",
    "boomPrepareTitle": "⏰ Preparação: Hora do Boom",
    "boomPrepareBody": "{{title}} começa em 5 minutos! Prepare-se.",
    "boomNowTitle": "🚨 HORA DO BOOM!",
    "boomNowBody": "{{title}} COMEÇOU AGORA!",
    "unlockPremiumTitle": "Desbloqueia o Premium",
    "unlockPremiumBody": "Escolhe Basic, Pro ou Premium e sai do plano gratuito.",
    "signalBuy": "COMPRA",
    "signalSell": "VENDA",
    "signalWaiting": "AGUARDAR",
    "tierPremium": "PREMIUM",
    "tierPro": "PRO",
    "tierFree": "GRÁTIS",
    "signalTitle": "{{emoji}} Novo Sinal {{tier}}: {{pair}} {{type}}",
    "signalBody": "Entrada: {{entry}} | SL: {{sl}} | TP: {{tp}}{{rr}}{{expires}}",
    "expires": " Expira: {{time}}"
  }
}
```

- [ ] **Step 1: `notifications.ts`**

`import { i18n } from '@/lib/i18n'`. Nomes de canais (`:23`, `:30`, `:222`) → `i18n.t('notifications.channelBoomWarn'|'channelBoomAlarm'|'channelPlans')`. Títulos/bodies (`:142-143`, `:159-160`, `:251-252`) → `i18n.t('notifications.boomPrepareTitle')` + `i18n.t('notifications.boomPrepareBody', { title })`, etc.

- [ ] **Step 2: `signalNotifications.ts`**

`import { i18n }` (já importa `Platform`). Canal `'Novos Sinais'` → `i18n.t('notifications.channelSignals')`. `formatSignalType` → `i18n.t('notifications.signalBuy'|'signalSell'|'signalWaiting')`. `getTierLabel` → `i18n.t('notifications.tierPremium'|'tierPro'|'tierFree')`. `Expira: HH:MM` → `i18n.t('notifications.expires', { time: new Date(signal.expiresAt).toLocaleTimeString(i18n.language, { hour: '2-digit', minute: '2-digit' }) })`. Title/body → `i18n.t('notifications.signalTitle', { emoji, tier: tierLabel, pair, type: typeLabel })` + `signalBody`.

- [ ] **Step 3: `boomPrefs.ts`**

`VOL_TIERS` **mantém-se como dados canónicos** (labels `Todas`/`Alta`/`Média`/`Baixa` usados como chave de filtro). A tradução visual é feita em `definicoes-booms.tsx` (Task 12, chaves `volAll`/`volHigh`/`volMedium`/`volLow`). **Sem alterações ao `boomPrefs.ts`.**

- [ ] **Step 4: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add src/lib/notifications.ts src/lib/signalNotifications.ts src/lib/i18n/locales/pt.json
git commit -m "feat(i18n): notificações (canais e conteúdo)"
```

---

### Task 18: Gerar os 13 dicionários + script de validação de chaves

**Files:**
- Modify: `src/lib/i18n/locales/en.json`, `es.json`, `fr.json`, `ja.json`, `ln.json`, `de.json`, `it.json`, `nl.json`, `zh.json`, `ko.json`, `ru.json`, `sw.json`, `ar.json`
- Create: `scripts/check-keys.mjs`

- [ ] **Step 1: Gerar as traduções**

Para cada idioma (en, es, fr, ja, ln, de, it, nl, zh, ko, ru, sw, ar), criar o dicionário **com o mesmo conjunto de chaves** de `pt.json`, com valores traduzidos:

- Convenções: `{{count}}`/`{{title}}`/`{{plan}}` etc. preservados; `%`/`$`/`€`/`Kz` preservados; `WAT`/`GMT`/`BOOM`/`TP`/`SL`/`RR`/`PIPS` (acrónimos) mantêm-se como estão; unidades "Kz/USD" não traduzidas.
- **Formato de datas por locale:** `format.longDate` e `format.datetimeWAT` com a ordem do idioma (pt: `{{day}} de {{month}} de {{year}}`; en: `{{month}} {{day}}, {{year}}`; es: `{{day}} de {{month}} de {{year}}`; fr: `{{day}} {{month}} {{year}}`; de: `{{day}}. {{month}} {{year}}`; it: `{{day}} {{month}} {{year}}`; nl: `{{day}} {{month}} {{year}}`; ja: `{{year}}年{{month}}月{{day}}日`; zh: `{{year}}年{{month}}月{{day}}日`; ko: `{{year}}년 {{month}} {{day}}일`; ru: `{{day}} {{month}} {{year}}`; ar: `{{day}} {{month}} {{year}}`; ln/sw: `{{day}} {{month}} {{year}}`). `datetimeWAT`: manter `às`/`at`/`à`/`um`/… conforme idioma e manter o sufixo `WAT` (pt: `{{dd}}/{{mm}}/{{yyyy}} às {{hh}}:{{min}} WAT`; en: `{{mm}}/{{dd}}/{{yyyy}} at {{hh}}:{{min}} WAT`; etc.).
- **Meses:** traduzir as 12 chaves `format.months.*` (ex.: en `January`…; ja `1月`…; zh `1月`…; ko `1월`…; ar com nomes árabes).
- **LN (Lingala) e SW (Suaíli):** qualidade aceitável mesmo que aproximada (idiomas de baixo recurso); manter acrónimos e interpolações corretas.
- **AR:** valores árabes; texto renderizado LTR (sem `I18nManager`).
- **Chave por chave:** copiar a estrutura exata de `pt.json` (mesmos caminhos); nenhuma chave a mais nem a menos.

- [ ] **Step 2: Criar `scripts/check-keys.mjs`**

```js
import { readFileSync, readdirSync } from 'node:fs';
import { join } from 'node:path';

const dir = 'src/lib/i18n/locales';
const files = readdirSync(dir).filter((f) => f.endsWith('.json'));
const ref = JSON.parse(readFileSync(join(dir, 'pt.json'), 'utf8'));

function collect(obj, prefix = '') {
  return Object.entries(obj).flatMap(([k, v]) => {
    const key = prefix ? `${prefix}.${k}` : k;
    return v && typeof v === 'object' ? collect(v, key) : [key];
  });
}

const refKeys = new Set(collect(ref));
let failed = false;

for (const f of files) {
  const data = JSON.parse(readFileSync(join(dir, f), 'utf8'));
  const keys = new Set(collect(data));
  const missing = [...refKeys].filter((k) => !keys.has(k));
  const extra = [...keys].filter((k) => !refKeys.has(k));
  if (missing.length || extra.length) {
    failed = true;
    console.error(`${f}: em falta=${missing.length} extra=${extra.length}`);
    if (missing.length) console.error('  em falta:', missing.slice(0, 10).join(', '));
    if (extra.length) console.error('  extra:', extra.slice(0, 10).join(', '));
  } else {
    console.log(`${f}: OK (${keys.size} chaves)`);
  }
}

process.exit(failed ? 1 : 0);
```

- [ ] **Step 3: Correr a validação**

Run: `node scripts/check-keys.mjs`
Expected: `pt.json: OK (N chaves)` e os 13 idiomas `OK` com o mesmo N. Se algum falhar, corrigir o dicionário e repetir.

- [ ] **Step 4: Verificação + commit**

Run: `npx tsc --noEmit` · Run: `npx expo lint` (ambos limpos)
```bash
git add src/lib/i18n/locales scripts/check-keys.mjs
git commit -m "feat(i18n): 14 dicionários completos + script de validação de chaves"
```

---

### Task 19: Verificação E2E final

**Files:**
- Run-only (sem alterações de código, exceto correções de bugs encontrados)

- [ ] **Step 1: Baseline estática**

Run: `npx tsc --noEmit` → limpo
Run: `npx expo lint` → 0 erros
Run: `node scripts/check-keys.mjs` → todos `OK`

- [ ] **Step 2: Verificar que não restam strings PT hardcoded**

Run (grep) e corrigir ocorrências restantes fora dos sítios autorizados (corpos legais):
```
rg -n "(['\"])[A-ZÀ-Ú][^'\"]*" src/app src/components --type tsx | grep -v 't(' | grep -v "require('@" 
```
Esperado: restam apenas os corpos legais em PT (Task 15), dados de domínio (pares/timeframes) e `PlanFeature` se não convertido. Corrigir qualquer string UI em falta com as chaves apropriadas de `pt.json`.

- [ ] **Step 3: E2E no browser (dev server + Playwright)**

1. `npx expo start` (web) e abrir `http://localhost:8081`.
2. Ir a **Perfil → Idioma** → selecionar **English** → confirmar re-render em EN (dashboard, tabs, perfil).
3. Selecionar **Lingála (ln)** e **العربية (ar)** → confirmar que renderizam (LTR) sem quebras.
4. Voltar a **Português** → UI em PT.
5. **Reload** → escolha persiste (`app_language` em AsyncStorage).
6. Mudar o idioma do sistema no browser (ou `expo` web) para um código não suportado → app mostra PT (fallback).
7. Verificar `horarios`/`analises`/`planos`/`comunidade` em EN sem strings PT visíveis (exceto corpos legais e conteúdo de domínio).

- [ ] **Step 4: Commit final (se houve correções)**

```bash
git add -A
git commit -m "fix(i18n): correções da verificação E2E"
```

- [ ] **Step 5: Reportar resultado**

Reportar ao dono do produto: sumário, contagem de chaves (`node scripts/check-keys.mjs`), resultado do E2E e confirmação de que `pt.json` continua a ser a fonte única.

---

## Fora de alcance (recordar ao executar)

- **RTL** para Árabe (renderiza LTR).
- Traduções comunitárias/por utilizador.
- Conteúdo do servidor/bot (mensagens de IA, conteúdo admin) — exceto labels de erro de `aiSupport`/`adminApi` já cobertos.
- Corpos legais completos (apenas títulos).
